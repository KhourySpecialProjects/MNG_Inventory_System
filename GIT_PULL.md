to do a pull request follow this command

git add .
git commit -m "MESSAGE HERE"
git checkout main
git checkout -b BRANCH_NAME_HERE_UNIQUE
git push -u origin BRANCH_NAME_HERE_UNIQUE

go to github and send the cr to someone
