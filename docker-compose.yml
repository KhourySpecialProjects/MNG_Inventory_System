services:
  dynamodb-local:
    image: amazon/dynamodb-local:latest
    container_name: mng-dynamodb-local
    ports:
      - '8000:8000'
    command: '-jar DynamoDBLocal.jar -sharedDb -inMemory'
    networks:
      - mng-local-network

networks:
  mng-local-network:
    driver: bridge
